<h1><%=link_to image_tag(redarrow.png"), :back %>All Users</h1>
<hr>
  <% @user.each do |user| %>
    
      <img class="smallround" src="<%= user.image %>">
      <p><%= link_to user.name, user %>
       <p><%= user.location %></p>
      <hr>
      <% end %>
